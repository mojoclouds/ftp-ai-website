---
layout: base.njk
title: "Blog | Sports Betting Analytics & Model Insights | FTP.ai"
description: "Articles on contrarian betting strategy, ML model methodology, sports market analysis, and the data science behind algorithmic sports predictions."
url: "/blog/"
activeNav: "blog"
permalink: "/blog/index.html"
---

  <!-- Page Header -->
  <section class="page-header">
    <div class="container">
      <span class="section-label fade-in">Insights</span>
      <h1 class="fade-in fade-in-delay-1">Blog</h1>
      <p class="fade-in fade-in-delay-2">Model methodology, market analysis, and the science behind data-driven sports predictions.</p>
    </div>
  </section>

  <!-- Blog Posts -->
  <section class="section" style="padding-top: 20px;">
    <div class="container">
      {% set posts = collections.blog %}
      {% if posts.length > 0 %}
      <div class="blog-list">
        {% for post in posts %}
        <a href="{{ post.url | url }}" class="blog-card fade-in">
          <div class="blog-card-content">
            <span class="blog-date">{{ post.date | dateFormat }}</span>
            <h2>{{ post.data.title }}</h2>
            <p>{{ post.data.excerpt }}</p>
            <span class="blog-read-more">Read more &rarr;</span>
          </div>
        </a>
        {% endfor %}
      </div>
      {% else %}
      <div class="coming-soon-block fade-in">
        <span class="big-icon">📝</span>
        <h2>Posts Coming Soon</h2>
        <p>We're working on our first articles covering model methodology, market analysis, and sports-specific insights. Check back soon.</p>

        <form class="email-signup" id="signupForm" novalidate>
          <input type="email" placeholder="Enter your email" id="emailInput" required>
          <button type="submit" class="btn btn-primary">Join the Waitlist</button>
        </form>

        <p style="font-size: 0.8rem; color: var(--text-muted); margin-top: 16px; margin-bottom: 0;">We'll only email you when we launch. No spam.</p>
      </div>
      {% endif %}
    </div>
  </section>
